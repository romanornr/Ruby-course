puts expand = defined?( expand ) ? expand : true

# defined? gives a string back. But when it can't be resolved
	# a nil is given back. 
	# code below does give the string back
# test = 'ruby'
# puts expand = defined?( test ) ? test : true
